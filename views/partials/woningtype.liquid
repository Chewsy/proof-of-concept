<div class="popover-space-between">
  <fieldset>
    <legend class="checkbox-legend">{{ legend }}</legend>
    <div>
      <input 
        type="checkbox" 
        id="{{ legend }}" 
        name="woningtype" 
        value="{{ legend }}" 
      />
      <label for="{{ legend }}">{{ label }}</label>
    </div>
  </fieldset>

  <span class="woningtype-aantal">1.000</span>
</div>